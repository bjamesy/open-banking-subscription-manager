# SubTrack Frontend

Placeholder. The frontend framework is not yet decided (architecture §6.6) — the cresidential
project used Vite + React + TypeScript, which is the likely default but not yet confirmed.

This directory exists so the repo is a monorepo (`backend/` + `frontend/`) from the start.
Scaffold the app here once the framework is chosen.
